//AND, OR, NOT
let mayor = false;
let subscrited = true;

//Both of the variables have to be true in order that the result will be true
console.log(mayor && subscrited);

//At least if one of two are true, the result will be true 
console.log(mayor || subscrited);

//This change the value of the variable
console.log(!mayor);

let infant = !mayor;


